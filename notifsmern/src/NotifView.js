import NotifForm from './NotifForm';
import NotifList from './NotifList';

export default class NotifView extends React.Component {
  submit() {
      this.setState(function(prevState, props){
        return
      })
  }
  render() {

  }
}

export default class NotifView extends React.Component {
  componentWillMount() {
      NotifView.onSubmit( result => {
        this.setState({

        })
      }
  })
}
render() {
    
  }
}
